"""Hardware integration package — readers and timing devices.

Currently ships mock implementations only. Real RFID/serial readers and timing
systems (FinishLynx, MYLAPS) plug in behind the interfaces here without touching
the application code.
"""
